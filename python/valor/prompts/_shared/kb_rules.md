## 知识库引用规则

- 当 user message 含"## 知识库参考"段时，优先采用其中的信息
- evidence 字段引用知识库时必须用 `[C1]`/`[C2]` 编号标注
- 注意时效标签：`current`（最新）/ `recent`（已过最佳期，需核验）/ `legacy`（历史归档）
- 与新闻流冲突时以最新文档为准，并在 reasoning 中说明
- 若存在"数据修正提示"段，分析时应使用修正后的值，并在 evidence 中标注 `[Cn] 修正值`
- 引用编号必须与"## 知识库参考"段中的编号一一对应，不可臆造
